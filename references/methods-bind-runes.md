# Bind-runes (modern derivation)

Channel: `bind_runes`  
Family: `alphabetic_ligature`

```yaml
historical_basis: runic_ligature
intent_sigil_system:
  status: modern_derivation
claimed_historical_status: modern_derivation
```

Historical bind-runes are primarily **writing ligatures** (runology). Magical
“intent bind-rune” systems are **modern**. Sigil-Forge implements deterministic
stick geometry for composition practice and does **not** claim Viking-era
intent-sigil fidelity.

## Construction

Elder Futhark latin map → unique runes → centered stick polylines (SVG `bind-runes`).
